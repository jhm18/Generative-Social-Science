The Pajek macro file contained in the main project file generates a set of partitions at varying resolutions. The macro produces a set of partition (.clu) files
that the routine analyzes.

These files will be generated by the macro file. You don't need to download them. They are here to illustrate the basic components of the worflow.
